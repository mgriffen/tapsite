Navigate to a URL and inspect the page. Returns compressed DOM with numbered interactive elements.

Use `tapsite_navigate` to go to the URL, then return the page structure and interactive elements list.

URL: $ARGUMENTS
