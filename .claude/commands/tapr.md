Export a full report for one or more URLs. Generates HTML report, Markdown report, JSON data, and screenshots.

Use `tapsite_export` with the provided URL(s). After export completes, list the files created in the output directory.

URL(s): $ARGUMENTS
